class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        # what makes this process so hard? lol

        actual_num1 = int(num1)
        actual_num2 = int(num2)

        actual_sum = actual_num1 * actual_num2

        return str(actual_sum)